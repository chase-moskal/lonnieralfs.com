{
	"private": true,
	"name": "lonnieralfs.com",
	"version": "0.1.0-dev.0",
	"description": "website for lonnie ralfs",
	"license": "ISC",
	"scripts": {
		"prepare": "run-s clean && run-p importmap && run-s test && echo ✔ done build",
		"clean": "rimraf dist && mkdirp dist",
		"importmap": "importly < importly.config > dist/importmap.json",
		"test": "exit 0",
		"start": "serve -n"
	},
	"devDependencies": {
		"importly": "0.0.0-dev.4",
		"mkdirp": "^0.5.1",
		"npm-run-all": "^4.1.5",
		"rimraf": "^2.6.3",
		"serve": "^11.0.0",
		"request": "^2.88.0"
	}
}
